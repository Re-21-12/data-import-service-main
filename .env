# DEPRECATED: este .env fue creado para docker-compose, pero el repositorio
# fue solicitado para conservar solo el Dockerfile y el workflow de GHCR.
# Por seguridad no mantengas credenciales en archivos dentro del repo. Si
# necesitas variables para CI/CD, configúralas en los Secrets del repositorio
# (GitHub Secrets) o en el proveedor de despliegue.

# Si quieres reutilizar las variables, vuelve a crear un .env localmente y
# no lo añadas al repo.
